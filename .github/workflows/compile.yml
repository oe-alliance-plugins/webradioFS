name: compile

on:
  push:
    branches: [ main ]
  pull_request:
    branches:
      - "*"

jobs:
  build:
    uses: oe-alliance-plugins/.github/.github/workflows/compile.yml@main
    # with:
    #   path: src
    #   python-versions: '["3.14"]'
